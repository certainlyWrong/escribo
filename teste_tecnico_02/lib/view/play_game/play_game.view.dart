import 'package:flutter/material.dart';

class PlayGameView extends StatelessWidget {
  const PlayGameView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
